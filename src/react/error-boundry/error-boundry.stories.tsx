import React from 'react';
import { ErrorBoundryExample } from './error-boundry';

export default {
    title: 'ErrorBoundry',
    component: ErrorBoundryExample
};

export const Basic = () => (
    <ErrorBoundryExample/>
);
